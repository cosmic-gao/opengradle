# =========================================================================
# Nacos dataId: global-variables.yaml
# Group:        DEFAULT_GROUP
# Description:  Optional cross-service shared configuration. Uncomment the
#               matching `shared-configs` block in bootstrap.yml to make
#               opengradle pull this file.
# =========================================================================

# Example: shared environment variables used by multiple gateway services.
common:
  org-name: "opengradle"
  contact-email: "ops@example.com"
